# 2026-08-31

## M0 项目初始化（完成）

用 `@skill:ai-coding-method` 的规范从零初始化了《蛛丝王权》项目。

**产出**：制品七件套（CLAUDE.md 唯一根入口 + 地图/不变量/术语表/工单/ADR×6/接力文件）、
工程四道门（TS strict / ESLint 9 红线规则 / Vitest 覆盖率棘轮 / CI + husky）、
核心玩法参考实现（`src/core/` 的 types/rng/stats/deck/game）、7 张种子卡 + 9 个结局、
47 个单元测试（core 覆盖率 95%+）。首个 commit：`fb59556`。

**关键设计决定**：先做「最小但真实」的参考实现（6~7 张卡跑通全部机制），
再在 M1 高委托扩到 40 张卡——避免先造抽象引擎、让抽象在真空里长歪。

**不变量下沉情况**：#1/#3/#5/#6/#7 已变成 ESLint 规则或测试断言；
#2（存档版本迁移）与 #8（IP 边界）待 M2/部署时再下沉为 L1。

## 修掉的 Git 推送障碍

用户 `git commit` 报 `npx: command not found`、`git push` 报 `src refspec main does not match any`。
两个独立问题：

1. 钩子失败 → 提交没生成 → 空仓库自然无处可推。根因是 **Git 钩子不继承交互式 shell 的 PATH**。
   重写 `.husky/pre-commit`：补 PATH 候选目录 + 直接调用本地 lint-staged 入口（绕开 npx）。
   用 `env -i PATH=... sh .husky/pre-commit` 模拟受限环境实测通过。
2. `git remote add origin` 报「已存在」是正常的——远端早就配好了，不需要重加。

## IP 定位反转（已落单为 ADR-0007）

查 GitHub API 确认远端是公开仓库，与 ADR-0006 冲突，停手上报。用户裁决：公开发布。
按「变更落单」把制品全部同步了一遍（不然下一棒 AI 会照着过期红线走）：
新增 **ADR-0007 取代 ADR-0006**、改写不变量 #8、改写 `CLAUDE.md` 原则 8/9、
补工单 T-001 变更记录、接力文件标记已裁决。commit `e1208d9`。

## 推送卡在认证（未解决，需用户操作）

`git push` 报 `could not read Username`：无凭据 + 我的 shell 没有 TTY，
GCM 弹不出登录窗（`bash: /dev/tty: No such device or address`）。
探测确认：`git credential fill` 无输出 = 凭据缓存里没有 github.com 的条目。

**用户偏好走 HTTPS 而非 SSH**，所以采用 HTTPS + Git Credential Manager 方案：
remote 保持 `https://github.com/JJCurrry/Spidey-Reigns.git` 不用改，
由用户在**自己的终端**跑 `git push -u origin main`，GCM 会弹浏览器完成 OAuth，
凭据之后存进 Windows 凭据管理器，后续我这边也能直接推。

环境事实（下次别重复排查）：
- `credential.helper = helper-selector`，实际选中 `manager`（GCM 已装，
  `/mingw64/bin/git-credential-manager` 与 `/c/Program Files/Git/mingw64/bin/git-credential-manager.exe` 都在）
- SSH 密钥 `~/.ssh/id_ed25519` 存在但**未注册到 GitHub**（`ssh -T` = Permission denied），仅作备选
- `gh` CLI 未安装
- 兜底方案：GitHub 生成 PAT，用户名填 JJCurrry、密码填 token

本地 3 个 commit（fb59556 / e1208d9 / 9cc7488）已就绪，等认证后一次推上去。

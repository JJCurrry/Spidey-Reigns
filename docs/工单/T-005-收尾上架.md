# 任务工单：T-005 收尾/上架（M3-C）

> 灵魂：把原型变成「可完整游玩」的游戏——标题屏 + 主角头像槽接入预留的 `char-spider-man`；部署发布的代码侧就绪（build 产出 `dist/`），实际 push/发布为 L3 仅由人执行（ADR-0007）。
> 检验标准：一个全新 AI 会话，只读 `CLAUDE.md` + `docs/工单/T-004-机制延伸.md` + 本工单 + `docs/接力文件.md`，能准确接着做后续（如有）。

## 委托级别

- [x] **中委托**，按里程碑分批交付、每批一个 commit。理由：
  - 高委托的部分：标题屏 UI、头像接线、图鉴入口、单测、build——全部机器可核对、可逆。
  - 需人过目的部分：标题文案/视觉是否舒服——属 `CLAUDE.md` 原则 3（手感/品味），AI 不自评。
- 中断条件（触发即停手报告，不自裁）：累计改动 >5 个非预期文件 / 想注释或删断言 /
  同一错误修 3 次 / 需要新增指标键或修改 `STAT_KEYS` 顺序（需新 ADR）/ 想给指标加「临界保护」（原则 7）/ 触发 push 到公开远端（L3）。

## 验收分级

- **A 级（机器判定）**：类型、lint、测试、覆盖率、构建、标题屏渲染、头像槽接入 `char-spider-man`、图鉴从标题可达、结束后可返回标题。
- **B 级（人判定，AI 不自评）**：标题文案/视觉是否舒服。

## 自动化级别

- [x] **L1 自动 PR**：生成后由人审一次再合入。不触碰认证 / 支付 / 生产 / 远端发布。
- ❌ 推送到公开远端 / 触发平台发布属 **L3，仅由人执行**（ADR-0007）。

## 入（前提 / 背景）

- 前提：M0/M1/M2/M3-B 全部完成，`npm run verify` 全绿（76 测试）；`char-spider-man` 预留槽已在 `src/content/assets.ts` 登记但 UI 未引用（见 T-004 的 out-of-scope 与 `ASSUMPTION-CODEX-ENTRY`）。
- 目标（M3-C）：① 标题屏；② 主角头像槽接入 `char-spider-man`；③ 部署发布代码侧就绪。
- 人在开工前的裁决（2026-08-31，continue 指令）：M3-B 完成后继续 **M3-C 收尾/上架**。
- 强约束（不得违反）：
  - 不变量 #8：不引入漫威官方素材；`char-spider-man` 为 M2 生成的荷兰弟式原创演绎（ADR-0009），沿用即可，不新增图片。
  - 原则 7：不削弱死亡判定 / 不新增指标缓冲。
  - 不变量 #4：内容只在 `src/content/`；本单不新增内容。

## 范围边界（In scope）

### 1. 标题屏（UI）
- `src/ui/TitleScreen.tsx` + `src/ui/title.css`：
  - 游戏标题「蛛丝王权」、一句副标语。
  - 主角半身像：`<AssetFrame assetId="char-spider-man" />`——即接入 T-004 预留、此前未引用的头像槽。
  - 「开始游戏」按钮 → `onStart()`。
  - 「图鉴」按钮 → `onShowCodex()`（复用 M3-B 的 `CodexScreen`）。
  - 羊皮纸主题沿用 `global.css` 的 CSS 变量。

### 2. 接线（App 编排）
- `src/App.tsx`：新增 `started` 门控——未开始显示 `TitleScreen`，开始后显示 `ReignGame`；`showCodex` 覆盖层从标题与游戏内均可达；游戏结束后经 `ReignGame → EndingScreen` 的 `onExit` 可「返回标题」。
- `src/ui/ReignGame.tsx`：新增可选 `onExit` 透传给 `EndingScreen`。
- `src/ui/EndingScreen.tsx`：新增可选 `onExit`，渲染「返回标题」按钮（与「再来一局」「查看图鉴」并列）。

### 3. 部署代码侧
- `npm run build` 产出 `dist/`（已可用）。注意：本沙箱内 `npm run build`/`coverage` 需前缀 `CODEBUDDY_SAFE_DELETE_ENABLED=0` 以绕过 `genie-trash` 安全删除 shim（详见 `docs/接力文件.md` 踩坑段），真实机器无 shim 时为 no-op。
- 实际 push / 平台发布由人执行（L3，ADR-0007）——本单不触发。

### 4. 测试
- `src/ui/TitleScreen.test.tsx`：渲染标题；`AssetFrame` 以 `char-spider-man` 接入（真图渲染或占位均算，不白屏）；「开始游戏」调用 `onStart`；「图鉴」调用 `onShowCodex`。
- 更新 `src/App.test.tsx`：因新增标题门，需先点「开始游戏」再断言四指标条与事件卡渲染；可选补一条「返回标题」链路。

### 5. 文档同步
- `docs/地图.md`：模块索引补 `TitleScreen.tsx` / `title.css`。
- `docs/接力文件.md`：标记 M3-C 进度与下一棒（若无后续）。

## Out of scope（与目标同等重要）

- ❌ 平衡数值的系统性调优（M3-A；属原则 3，需新工单与调参规格）。
- ❌ 新增死法 / 卡 / flag 链（M3-B 已完成）。
- ❌ push 到公开远端 / 触发平台发布（L3 仅人）。
- ❌ 削弱或缓冲死亡判定（原则 7）；新增反派/数值「临界保护」。
- ❌ bump `SAVE_VERSION`（本单不碰 `SaveData` 结构）。
- ❌ 引入漫威官方素材 / 新增图片资源（沿用 M2 真图与 `char-spider-man`）。

## 出（验收 / DoD —— A 级全部机器可核对）

### 四道门
- [ ] `npm run verify` 全绿（类型 + lint + 测试）。
- [ ] `npm run build` 成功（沙箱内加 `CODEBUDDY_SAFE_DELETE_ENABLED=0`）。
- [ ] `npm run coverage`：`src/core/**` 不低于棘轮阈值 80%（只进不退）。
- [ ] `npm run format:check` 通过。

### 标题屏与头像
- [ ] `TitleScreen` 渲染标题 + 主角头像（`AssetFrame` id=`char-spider-man`，不白屏不抛错）。
- [ ] 「开始游戏」调用 `onStart`；「图鉴」调用 `onShowCodex`。
- [ ] `App` 标题门控：未开始显示标题，开始后进入游戏；图鉴从标题与游戏内均可达；结束后可「返回标题」。

### 合规
- [ ] 不引入任何漫威官方素材；头像沿用 M2 的 `char-spider-man` 原创演绎。
- [ ] 死亡判定未被削弱（无新增临界保护 / 无指标缓冲）。

### 交接
- [ ] `docs/接力文件.md` 已更新，下一棒只读 `CLAUDE.md` + 本工单 + 接力文件能接后续。
- [ ] `docs/地图.md` 已同步新模块。

## 过程（里程碑，一个里程碑 = 一次提交，diff ≤400 行）

- **M3-c1**：本工单 + `docs/地图.md` / `docs/接力文件.md` 指针同步（纯文档）。
- **M3-c2**：UI —— `TitleScreen.tsx` + `title.css` + App/ReignGame/EndingScreen 接线。
- **M3-c3**：测试 —— `TitleScreen.test.tsx` + 更新 `App.test.tsx`。
- **M3-c4**：收尾 —— 接力/地图同步收口 + `verify`/`build`/`coverage`/`format:check` 四门全绿。
- 黄金路径：`src/core/deck.ts`（不变量执行点）、`src/ui/CodexScreen.tsx`（覆盖层模式）、`src/ui/AssetFrame.tsx`（占位降级）。

## 假设与默认值（人已逐项核对；代码内用 `ASSUMPTION-<ID>` 标记）

- `ASSUMPTION-TITLE-EXIT`：**标题屏在开始与「返回标题」两处可达**；图鉴从标题与游戏内均可达。
  依据：最小化改动面，复用既有 `ReignGame` 编排与 `save` 数据、`CodexScreen` 覆盖层。
- `ASSUMPTION-TITLE-CONTENT`：**标题文案/标语** AI 给默认措辞（「蛛丝王权」+ 一句副标语），人可改（原则 3，AI 不自评）。

## 隐蔽工程标记

- [ ] 涉及数据库 schema / 核心接口契约 / 认证授权模型？**否** —— 无后端、无鉴权、无数据库。
- [ ] 涉及持久化 schema 变更？**否** —— 本单不碰 `SaveData` 结构，`SAVE_VERSION` 不变；`migrate.ts` 无需改动。

## 变更记录（任何口头/会话内变更必须落此处）

- 2026-08-31 用户（continue 指令）：M3-B 完成后继续 **M3-C 收尾/上架**；确认 AI 先写本工单（验收先行）再实现。
- 2026-08-31 M3-c1：立本工单 `T-005` + 同步 `docs/地图.md` / `docs/接力文件.md` 指针。

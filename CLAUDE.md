# 蛛丝王权（Spidey-Regins）项目规则

> 本文件是每个会话自动读取的「项目宪法 + 唯一根入口」。保持 ≤100 行。
> 只放两类东西：① 无法写成检查的「判断性原则」；② 指向其他制品的「指针」+ 一条仲裁序。
> 能写成失败检查的规则，一律不写在这里——已下沉为 ESLint 规则与 Vitest 守卫，见 `docs/不变量.md`。
> WorkBuddy/CodeBuddy 环境请读 `CODEBUDDY.md`，它是本文件的指针镜像，事实源仍是本文件。

## 一句话是什么
以蜘蛛侠为主角的《Reigns》式卡牌治国游戏：左右滑动做抉择，四个指标互相牵制，任一指标触及 0 或 100 即结束一局。技术栈 TypeScript + Vite + React；核心玩法为纯逻辑、零 UI 依赖。

## 常用命令
- 安装：`npm install`
- 运行：`npm run dev`
- 构建：`npm run build`
- 测试：`npm test`（单次）/ `npm run test:watch`
- 覆盖率：`npm run coverage`
- lint：`npm run lint` / `npm run lint:fix`
- 格式化：`npm run format`
- **全量门（提交前必过）**：`npm run verify`（= 类型检查 + lint + 测试）

## 判断性原则（仅放无法自动化的；≤15 条）
1. 不确定就停下问，不要猜测后硬上。
2. 发现工单 / 不变量 / 代码三者冲突时，停手报告，不自行仲裁。
3. 玩法手感（数值曲线、死法节奏、文案语气）属于人的品味判断，AI 不得自行「优化」；改动需新开工单。
4. 内容与引擎分离：新增剧情只改 `src/content/`，不写进逻辑；改玩法只改 `src/core/`，不碰文案。
5. 核心玩法逻辑（`src/core/`）保持纯函数、可测；任何必须依赖浏览器环境的需求，先与人对齐再落地。
6. 一局必须始终可完结：任何新增系统都不得产生「既无法继续、也不会死亡」的卡死状态。
7. 死亡是设计内容而非错误：不得为了让玩家活得更久，而在引擎层偷偷削弱死亡判定。
8. 本项目为**公开仓库**，IP 风险由人自担（见 ADR-0007，它取代了 ADR-0006 的「不公开发布」）。
   AI 不重复劝阻；但新增美术资源入库前，须提醒对照不变量 #8 自检一次。
9. 禁止把漫威官方素材（官方剧照 / 海报 / 商标 / logo / 官方字体）放进仓库或构建产物；
   仓库内只允许 AI 生成或自绘的原创演绎，以及文字层面的角色名。
10. 提交信息用中文，格式为 `M<里程碑>: <做了什么>`；一个里程碑一次提交，diff ≤400 行。

## 一张地图（指针，不是内容）
- 架构与模块索引：见 `docs/地图.md`
- 红线（不变量）：见 `docs/不变量.md`
- 术语（只收望文生义会错的词）：见 `docs/术语表.md`
- 设计决策（为什么这样）：见 `docs/adr/`
- 跨会话交接棒：见 `docs/接力文件.md`
- 工单目录：`docs/工单/`
- **参考实现 / 黄金路径**：`src/core/deck.ts`（纯函数 + 随机源注入 + 显式边界处理的样板，照此新增）
- 隔离区：暂无（新项目；随代码增长由园丁维护）

## 仲裁序（信息源冲突时谁先读）
任务工单 > 不变量清单 > 本文件原则 > 专项规范（ADR / 术语表） > 代码现状

> 仅排「信息源」先后，不排「权限」。真撞红线级冲突 → 停手并在接力文件上报，交人裁决，不自裁。
